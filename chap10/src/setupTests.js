// jest-dom adds custom jest matchers for asserting on DOM nodes.
// allows you to do things like:
// expect(element).toHaveTextContent(/react/i)
// learn more: https://github.com/testing-library/jest-dom
import '@testing-library/jest-dom/extend-expect';



// function createHTMLElement(tag, className, id, name, value, checked) {
//     var newElement = document.createElement(tag);
//     newElement.classList.add(className);
//     newElement.id = id;
//     newElement.name = name;
//     value ? newElement.value = value : null;
//     checked ? newElement.checked = checked : null;

//     return newElement;
// }
// document.querySelector('html').appendChild(createHTMLElement('input', 'inputTag', 'id', 'userId', 'ㅋㅋ'));